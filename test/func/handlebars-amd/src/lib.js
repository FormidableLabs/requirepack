define([
  "jquery",                             // Vendor path
  "./views/header",                     // Relative path (with nested dep)
  "hbs!custom-alias/templates/message"  // Custom app prefixed path (with nested prefixed dep),
], function () {});
